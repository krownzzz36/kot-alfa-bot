# HANDOFF → чат «Обновление пульта»: автозащита от бочки

## Задача
Реализовать в `holop_smash.py` авто-защиту от бочки (динамита) @holop по РЕАЛЬНОМУ сценарию,
снятому вживую 27.07.2026 (два боевых инцидента: обезврежено + взрыв с полным восстановлением).

## Где всё сырьё
- Полный лог с точными экранами и кнопками: `holop-panel/bomb_capture_20260727.md` (ОТКРОЙ ЕГО ПЕРВЫМ).
- Память: `holop-bomb-defense` (обновить по факту), `holop-fix-workflow` (процесс), `holop-smash-bot`.

## Процесс фикса (обязательно)
Репо = единственный источник правды. Правим ТОЛЬКО в `holop-panel/` (kot-alfa-bot) →
бамп VERSION → компиляция → `git commit` + `git push origin main`. Владимир и друзья тянут через update.py.
Сообщение в чат «Альфа-тестеры бота холопа ))» (chat_id -5160104813) с префикса «Клод Владимира:».

## ФИНАЛЬНАЯ логика (с правками Владимира)

### Обезвреживание каждой бочки
1. Поймать сообщение «💣 ЗАМИНИРОВАНО!» → взять message_id.
2. Нажать кнопку с подстрокой «огниво» (жать по ИНДЕКСУ 0 — текст с «—» ломает press).
3. То же сообщение → нажать кнопку с подстрокой «красн» (фитиль; простой ТЕКСТ «Красный фитиль»,
   БЕЗ эмодзи 🔴 — не полагаться на кружок!).
4. Читать текст того же сообщения: «ОБЕЗВРЕЖЕНА» → успех; «ВЗРЫВ» → восстановление.
- ⭐ «Мастер», «Кандалы», «25⭐» — НИКОГДА не трогать.
- Красный НЕ гарантирован (шанс 33%) — на промах обязателен путь восстановления.

### Огниво
Покупку Огнива УБРАТЬ из кода полностью. Владимир держит запас вручную (10–20 шт.).
Нет Огнива → не покупать, просто дать взорваться и лечить.

### Восстановление после взрыва (отдельный самодостаточный процесс)
1. ЛЕЧЕНИЕ территории: снять из казны РОВНО 100 000 серебра → нажать кнопку `100.0K🪙`
   (есть на экране взрыва и на экране Территории). Успех-alert: «✅ Территория восстановлена! -100000🪙».
2. ЗАЩИТА холопов: снять из казны 10% ЗОЛОТА (кнопка `10%`, НЕ фикс-сумма) →
   `Холопы` → `Холопы (N)` → `Защитить всех (N) за N×120 золота`. Не хватило → снять ещё 10%, повторить.
   Успех-alert: «🛡️ Защита установлена на N холопов! Потрачено: …».
3. ВОЗВРАТ: `Личная казна` → `Серебро → Депозит → Положить всё` и `Золото → Депозит → Положить всё`
   (весь свободный остаток обратно в депозит).
Ночью / в любой непонятной ситуации: взорвался → лечим → защищаем → вернули → всё. Территорию в цикле НЕ тыкать.

## Точные флоу казны (снято вживую)
- Взрыв, аварийный вид: `Личная казна` → `Снять с депозита (…🪙)` → `Снять 100.0K🪙`.
- Обычный вид (после лечения): `Личная казна` → `Золото (5%/день)` → `Снять` → `10%` (или `Ввести сумму`→число).
- Возврат: раздел валюты → `Депозит` → `Положить всё`.

## Что переписать в holop_smash.py
- `check_and_handle_bomb`/`handle_bomb`: действовать на самом сообщении «ЗАМИНИРОВАНО», а не открывать
  отдельный экран «Дружина». Матч Огнива и фитиля — по подстроке, нажатие по индексу.
- Красный фитиль: матч по «красн», убрать зависимость от эмодзи 🔴.
- Убрать `ensure_ognivo` (покупку Огнива).
- `recover_after_explosion`: лечение (100k серебра из казны) + защита (10% золота из казны) + возврат остатка в депозит.
- Поддержать ДВА вида казны: аварийный (при взрыве) и обычный (после лечения).
